---
id: fab-8468
status: closed
deps: [fab-l36k]
links: []
created: 2026-03-24T14:25:48Z
type: task
priority: 2
assignee: Ronny Unger
tags: [daemon]
---
# Implement claudeProcess — start, init handshake, query

Create `internal/agentcli/daemon.go` with unexported `claudeProcess` struct: `startClaudeProcess`, `sendInitMessage`, `waitForInit`, `waitForResult`, `query` (mutex-serialized). Port from clai `internal/claude/daemon.go`. Add `filterEnv` helper.

**Daemon-specific arg construction (pm-20260324-002):** Do NOT pass `Backend.Args` directly — it contains `-p` which is incompatible with `--input-format stream-json`. Add `buildDaemonArgs(cfg DaemonConfig)` that extracts model from `Backend.Args` (find `--model` flag value) and constructs: `--print --model <model> --input-format stream-json --output-format stream-json`. Add `extractModelFromArgs(args []string) string` helper.

**Scanner buffer (pm-20260324-003):** Set 1MB buffer on stdout scanner:
```go
scanner := bufio.NewScanner(stdout)
scanner.Buffer(make([]byte, 1024*1024), 1024*1024)
```
Default 64KB will truncate large judge responses.

**Env filtering:** `cmd.Env = filterEnv(os.Environ(), "CLAUDECODE")`

## Acceptance Criteria

- `claudeProcess` can start a fixture process, complete init handshake, and send/receive queries
- `buildDaemonArgs` extracts model from backend args, constructs stream-json flags
- Scanner uses 1MB buffer
- `CLAUDECODE` env vars filtered from subprocess

