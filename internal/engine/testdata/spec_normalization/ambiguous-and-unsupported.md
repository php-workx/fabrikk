The system might support importing remote work items later, but the source is not clear enough to implement now.

Ask the user before sending any private spec text to an external model.

Unsupported addition example for verifier fixtures: a normalized artifact must not claim that production deployment is in scope.
