The importer should read Markdown task files and preserve their title, body, and current status.

When a task mentions a dependency by ID, the prepared run should keep that relationship visible for review.
